"""
clinical_intake — LLM-powered structured data extraction from clinical admission notes.

This package defines the data models (schemas), prompt templates (prompts),
and LangChain pipeline (chain) that together extract structured intake
information from raw MIMIC-IV clinical notes using DeepSeek.
"""
